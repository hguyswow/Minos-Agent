# Minos Memory Engine - Architecture & Development Guidelines

이 문서는 Minos Memory Engine(알쫑이 에이전트)의 향후 코딩 및 유지보수를 담당할 **모든 AI 코딩 에이전트를 위한 절대적인 아키텍처 가이드라인**입니다. 코드 수정이나 새로운 기능 추가 시 반드시 아래의 철학과 구조를 준수해야 합니다.

---

## 1. 하드웨어 제약 및 기본 철학 (Hardware Constraints)
- **Target Spec:** RTX 3060 6GB VRAM (Laptop)
- **Core Philosophy:** GPU(VRAM) 자원은 극도로 제한적이므로, 무거운 LLM(대뇌)의 개입을 최소화하고 대부분의 데이터 수집, 필터링, 연산은 CPU/RAM 기반의 가벼운 파이썬 스크립트(자율신경계)로 **오프로딩(Off-loading)**해야 합니다.
- **판단 기준:** "이 작업을 굳이 LLM(GPU)이 컨텍스트를 소모하며 판단해야 하는가? 아니면 단순 파이썬 코드(CPU)로 쳐낼 수 있는가?"를 먼저 고민하십시오. 가급적 파이썬 코드로 해결하십시오.

---

## 2. 핵심 아키텍처: 자율신경계 시스템 (Nervous System Model)
현재 시스템은 인간의 신경계를 모방하여 2개의 독립된 프로세스로 돌아갑니다.

### A. 대뇌: 메인 엔진 (`antigravity_telegram.py` & `skill_system/skills/`)
- **역할:** 의식적인 사고, 사용자와의 대화, 고차원적 판단, 그리고 보조 스크립트들의 자가 치유(디버깅) 코딩.
- **🚨 절대 안전 수칙 (수정 금지):** `skill_system/skills/` 안에 있는 **메인 스킬 파일들은 AI의 핵심 뇌세포**입니다. 향후 어떤 AI 에이전트라도 **사용자의 명시적이고 구체적인 승인 없이는 이 파일들을 절대로 임의 수정하거나 덮어써서는 안 됩니다.**

### B. 자율신경계: 문어발 데몬 (`tentacles/tentacle_daemon.py` & `tentacles/`)
- **역할:** 24시간 윈도우 백그라운드에서 동작하며, 텔레그램 봇과는 별개의 프로세스(두 번째 심장)로 돌아가는 보조 정보 수집 스크립트들.
- **동작 방식:** `tentacle_daemon.py`가 하위 파이썬 스크립트들을 1분 주기로 무한 반복 실행합니다.
- **🛠️ 문어발 설계 원칙 (효율 극대화 3대 전략):**
  1. **사전 필터링:** LLM에게 길고 복잡한 원본 텍스트(HTML 등)를 던지지 마십시오. 문어발 파이썬 코드가 핵심 데이터 3줄만 정제해서 JSON이나 텍스트 파일로 저장해 두어야 합니다.
  2. **침묵의 감시자 (Threshold Trigger):** 데이터가 단순히 업데이트되었다고 LLM을 깨우지 마십시오. 특정 조건(예: 온도 급변, 에러 팝업 발생)이 달성되었을 때만 시스템 알림을 통해 LLM을 깨워 전력을 아끼십시오.
  3. **자체 쿨타임 로직:** 문어발 스크립트의 실행 주기를 늘리고 싶을 때, 절대 메인 스킬인 `Schedule_Manager`를 사용하지 마십시오. 문어발 스크립트(.py) 내부에 시간 체크 로직(예: `sys.exit()`)을 짜넣어 스크립트 스스로 실행 여부를 판단하게 하십시오.

---

## 3. 자가 치유 시스템 (Self-Healing Mechanism)
- 문어발 스크립트 실행 중 에러가 발생하면, 데몬이 `tentacles/logs/tentacle_errors.json`에 Traceback 에러 로그를 남깁니다.
- 텔레그램 봇 메인 엔진(`antigravity_telegram.py`의 `tentacle_error_checker`)이 이 비명 소리를 감지하고 AI 에이전트에게 긴급 알림을 쏩니다.
- 알림을 받은 AI 에이전트는 즉시 **`tentacle_manager.py`** 스킬을 사용하여 해당 문어발 코드를 읽어오고(read), 버그를 디버깅한 뒤 다시 배포(write)하여 문제를 자율적으로 해결해야 합니다. 문어발 수정은 사용자의 허락 없이도 즉각 진행하여 시스템을 복구하십시오.

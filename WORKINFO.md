# WORKINFO.md (작업 일지)

## 📌 오늘 작업 내역 (Today's Work) - 2026-04-27
* **웹 기반 스킬 편집기 (Web Skill Editor) 구축:** 
  * 원격 데스크톱 없이 모바일/태블릿에서도 대시보드를 통해 봇의 스킬(.py) 코드를 직접 편집하고 저장할 수 있는 기능 완벽 구현.
  * `dashboard_server.py`에 읽기/쓰기 API 추가 및 `index.html`에 CodeMirror 적용 완료.
* **텔레그램 토큰 증발 버그 패치:** 
  * `bot_config.json`을 읽을 때 `utf-8-sig` 인코딩 처리 누락으로 인해, 대시보드 환경설정 변경 시 기존 토큰이 초기화되는 심각한 오류 완벽 해결.
* **TTS (음성 출력) 모듈 분리 및 스레드 충돌 픽스:**
  * 텔레그램 스크립트에 종속되어 있던 TTS 기능을 `tts_engine.py`로 분리.
  * 윈도우 스피커 장치(COM 객체)를 백그라운드 스레드에서 접근 시 뻗어버리는 문제(`CoInitialize` 누락) 해결.
  * 이제 텔레그램뿐만 아니라 웹 대시보드에서 챗을 치더라도 알쫑이가 목소리로 대답함.

## 🚀 다음 세션 목표 (Next Session)
* **능동형 알림 시스템 (Proactive Agent):** 문어발(Tentacles) 스크립트에서 조건 달성 시 즉시 텔레그램으로 먼저 말을 거는 기능 구현 검토.
* **시각 기능 (Vision AI):** 이미지 분석 기능 등 확장 로드맵 추진.
* **기억 조작 대시보드 (Memory Editor):** 대시보드 내에서 장기 기억(ChromaDB)을 삭제/수정하는 UI 제작 (예정).
